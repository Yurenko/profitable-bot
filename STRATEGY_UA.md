# Логіка стратегії (простими словами)

## Що робить бот

**Long-only** — тільки покупки (лонг), без шортів.

### 1. Вхід (коли немає позиції)

Відкриває лонг, якщо **одночасно**:

- RSI(14) на 1m < 30 (перепроданість)
- RSI(14) на 5m < 35
- ADX < ~38 (немає дуже сильного тренду проти нас)
- ATR не в топ-10% аномальної волатильності
- Funding + Open Interest не «перегріті»
- Немає high-impact новин ±15 хв
- Не перевищено ліміт корельованих пар

Розмір: **~2% депозиту** як маржа на вхід, плече **5x** (isolated).

### 2. Якщо ціна падає — DCA (докупка)

- **Немає стоп-лосу** — позицію в мінусі **не закривають**.
- Якщо RSI на 15m < 30 і ціна пішла проти нас ще на ≥0.5% — додає ще ~2% equity.
- Максимум **4 рівні** (1 вхід + 3 докупки).

### 3. Захист від ліквідації (не стоп-лос!)

- Бот **додає маржу** (margin top-up), коли ціна наближається до ліквідації.
- Це **не продаж** — позиція залишається відкритою.

### 4. Вихід з позиції

| Режим | Поведінка |
|-------|-----------|
| **long_term_mode: true** (зараз) | Закриття **тільки в плюсі** за Take Profit (1% або 2%). Немає trailing, немає часткового закриття. |
| **auto_take_profit: false** | Взагалі не закриває автоматично — тільки DCA + top-up маржі (утримання роками). |
| **use_stop_loss** | Завжди `false` — продаж у збиток **не реалізований** і не буде без вашої згоди. |

### 5. Circuit breaker / daily loss

- Це **не стоп-лос** по позиції.
- Лише **зупиняє нові входи**, якщо день/депозит у великому мінусі.
- Відкриті позиції **не закриваються**.

---

## Монети ($100)

У `config.yaml` зараз альткоїни: SOL, ATOM, NEAR, LINK, AVAX, DOT, ADA, INJ.

Одночасно відкрито буде **до 2** сильно корельованих пар (ліміт ризику на малий депозит).

---

## Grid DCA (як на скріні Bybit)

З прикладу (розміри 0.07→0.09→0.11→0.13→0.17, ціни ~2046→1966→1859):

| Параметр | Значення зі скріну | У боті |
|----------|-------------------|--------|
| Крок між лімітами | ~3.2–5.5% (середнє ~4%) | `grid_step_pct: 0.04` |
| Множник розміру | ~1.18–1.31 (геом. середнє **≈1.25**) | `grid_size_multiplier: 1.25` |

Після ринкового входу бот будує сітку лімітів нижче entry:
`price_n = entry × (1 − step)^n`, `qty_n = base × multiplier^n`.

Режим: `dca_mode: grid` (або `rsi` — старий варіант по RSI 15m).


1. **Trailing TP** — закривав при відкаті після невеликого плюсу → **вимкнено** в long_term_mode.
2. **Circuit breaker** — лише пауза торгівлі, не продаж.
3. **Бектест** — в кінці тесту позиція примусово закривається для підрахунку статистики (тільки в backtest, не в live).

---

## Налаштування в Dashboard

Вкладка **Налаштування**:

- `long_term_mode` — довгострок без trailing/часткового TP
- `auto_take_profit` — вимкніть, якщо хочете тримати без автозакриття в плюсі
- `tp_min_pct` — 0.01 = 1% тейк-профіт від середньої ціни входу
